import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  root: {},
  cardMedia: {},
  focusHighlight: {},
  content: {
    padding: 0,
  },
  image: {
    objectFit: "contain",
  },
}));

export default useStyles;
