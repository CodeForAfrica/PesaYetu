import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  root: {},
  cardMedia: {},
  content: {
    padding: 0,
  },
  image: {
    objectFit: "contain",
  },
}));

export default useStyles;
