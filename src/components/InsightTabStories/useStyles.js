import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  root: {},
  media: {
    background: "white",
    padding: "2rem",
  },
}));

export default useStyles;
