import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  root: {},
  typeContent: {
    background: "#F0F0F0",
    opacity: 1,
    textAlign: "center",
    padding: "12px",
    fontSize: "10px",
    color: "#666666",
    letterSpacing: "0.8px",
    fontWeight: "600",
  },
  dataTypes: {},
}));

export default useStyles;
