import React from "react";

import Loading from ".";

export default {
  title: "Components/Loading",
  argTypes: {},
};

function Template() {
  return <Loading />;
}

export const Default = Template.bind({});

Default.args = {};
